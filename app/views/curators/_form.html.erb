<%= form_for @curator do |f| %>
    <%= f.label :user_id %>: 
    <%= f.collection_select(:user_id, User.all, :id, :username) %><br/><br/>
    <%= f.label :museum_id %>:
    <%= f.collection_select(:museum_id, Museum.all, :id, :name) %><br/><br/>
    <%= f.submit %>
    <% end %>